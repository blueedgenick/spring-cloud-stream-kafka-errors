spring:
  cloud:
    stream:
      bindings:
        output:
          destination: scstest
          content-type: application/json
          producer:
            error-channel-enabled: true
      kafka:
        binder:
          brokers: localhost
